{
  "name": "applyt",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted, single-user job application tracker (Phase 1: manual tracker).",
  "license": "MIT",
  "workspaces": [
    "backend",
    "dashboard",
    "extension"
  ],
  "scripts": {
    "dev": "concurrently -n backend,dashboard -c blue,magenta \"npm:dev:backend\" \"npm:dev:dashboard\"",
    "dev:backend": "npm run dev --workspace backend",
    "dev:dashboard": "npm run dev --workspace dashboard",
    "dev:extension": "npm run dev --workspace extension",
    "build": "npm run build --workspace backend && npm run build --workspace dashboard && npm run build --workspace extension",
    "test": "npm test --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
