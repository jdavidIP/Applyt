{
  "_comment": "Indeed's markup changes frequently without notice. Fix breakage here, not in indeed.ts. Prefer text matches over class names — the English confirmation copy churns far less than CSS classes.",

  "jobTitleSelectors": [
    "[data-testid='jobsearch-JobInfoHeader-title']",
    "h1.jobsearch-JobInfoHeader-title",
    "h1"
  ],

  "companySelectors": [
    "[data-testid='inlineHeader-companyName']",
    "[data-testid='jobsearch-CompanyInfoContainer'] a",
    ".jobsearch-CompanyInfoContainer"
  ],

  "jobDescriptionSelectors": [
    "#jobDescriptionText",
    "[data-testid='jobsearch-JobComponent-description']"
  ],

  "_locationModalityComment": "Verified live 2026-08-13 against a real posting (indeed.com/viewjob). Location has a stable data-testid. The workplace-type chip ('Hybrid work' / 'Remote' / etc.) has NO stable selector of its own — it's a bare unlabeled div — so it's matched by text, scoped to the company-info header block to avoid false-matching the job description body.",

  "locationSelectors": [
    "[data-testid='job-location']",
    "[data-testid='inlineHeader-companyLocation']"
  ],

  "modalityContainerSelectors": [
    "[data-testid='jobsearch-CompanyInfoContainer']",
    ".jobsearch-CompanyInfoContainer"
  ],

  "modalityTextMatches": {
    "hybrid": ["hybrid"],
    "remote": ["remote"],
    "on_site": ["on-site", "onsite", "in person", "in-person"]
  },

  "inPlatformApplyButtonSelectors": [
    "#indeedApplyButton",
    "[data-testid='indeedApplyButton']",
    "button[id*='indeedApplyButton']"
  ],

  "externalApplyButtonSelectors": [
    "[data-testid='apply-button-container'] a[href]",
    ".jobsearch-IndeedApplyButton-newDesign a[href]"
  ],

  "externalApplyTextMatches": [
    "apply on company site",
    "apply on employer site",
    "apply on company website",
    "apply on company's website"
  ],

  "confirmationContainerSelectors": ["#ia-container", "main", "#appcontent", "body"],

  "confirmationTextMatches": [
    "application submitted",
    "your application has been submitted",
    "application sent",
    "you applied on"
  ]
}
